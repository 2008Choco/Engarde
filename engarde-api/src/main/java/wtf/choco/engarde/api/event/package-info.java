/**
 * Optional {@link org.bukkit.event.Event Events} that may be called by plugins
 * that implement the {@link wtf.choco.engarde.api.AntiCheatService AntiCheatService}.
 * There is no guarantee that implementations will call these events, nor is it required.
 */
package wtf.choco.engarde.api.event;
